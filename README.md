For å starte serveren så kjører du bare "start_server.bat"
For å stoppe serveren så trykker du på "Ctrl + C", å skriver inn "y".

Nettsiden er på http://localhost:3000/booking

(Då må mulignes laste ned Node.js og Express.js)

